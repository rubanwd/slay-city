/**
 * Aspect ratio (width / height) admins crop a district background to, matched
 * to the portrait map area on `/map`. Single source of truth so the cropper
 * and any preview stay in sync — change it here if the map proportions change.
 */
export const MAP_ASPECT = 3 / 4;
